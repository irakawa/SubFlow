<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.SubFlow" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">#0A0A0A</item>
        <item name="android:statusBarColor">#0A0A0A</item>
        <item name="android:navigationBarColor">#0A0A0A</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Branded launch: dark backdrop with the Flow-S mark, hands off to Theme.SubFlow -->
    <style name="Theme.SubFlow.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#0A0A0A</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.SubFlow</item>
    </style>
</resources>
